package infra;

import core.University;
import core.UniversityRepository;

import java.util.UUID;

public class UniversityRepositoryImpl implements UniversityRepository {
    @Override
    public University getById(UUID universityId) {
        return null;
    }
}
